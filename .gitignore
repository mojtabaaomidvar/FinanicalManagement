# ── Dependencies / build ──────────────────────────────────
node_modules/
dist/
*.tsbuildinfo

# ── Deploy / tooling caches ───────────────────────────────
.vercel/
.qwen/
# کانفیگ شخصیِ اسکریپتِ دیپلوی (آدرس SSH سرور) — نباید وارد گیت شود
deploy-config.bat

# ── OS junk ───────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Environment secrets ───────────────────────────────────
# هیچ فایل env واقعی نباید کامیت شود؛ فقط نمونه مجاز است.
.env
.env.*
!.env.example

# ── Python backend (backend/) ─────────────────────────────
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/

# ── فایل‌های آپلودیِ زمان‌اجرا (استوریجِ محلیِ فاز ۸) ──────
# عکس‌های کاربر نباید در گیت ذخیره شوند؛ روی سرور زیرِ storage_dir می‌مانند.
backend/var/
var/uploads/
