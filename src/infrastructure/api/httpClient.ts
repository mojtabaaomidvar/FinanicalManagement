/* بازنشسته در فاز ۹ (سوییچِ کامل و تمیز).
   ────────────────────────────────────────────────────────────────────────
   این ماژول seamِ شبکهٔ قدیمی بود: تابعِ rpc() با دو مسیرِ ارتباطی —
     • rpcDirect: مستقیم به PostgREST سوپابیس (خارج از ایران)
     • rpcProxy: از طریقِ تابعِ سرورلسِ «/api/rpc» برای عبور از مسدودیِ supabase.co
   به‌همراهِ سنجشِ همزمانِ مسیرها (probeMode)، ذخیرهٔ مسیرِ برنده در
   localStorage با کلیدِ «mali-man.rpcMode» و نگاشتِ خطا (RPC_ERROR_MAP).

   با مهاجرت به بک‌اندِ اختصاصیِ داخلِ ایران، این معماریِ دومسیره لازم نیست: همهٔ
   درخواست‌ها از «RestClient» (infrastructure/api/restClient.ts) و مبدأِ واحدِ
   «shared/config/apiUrl» می‌روند و نگاشتِ خطا هم به RestClient منتقل شد
   (با اولویتِ «کدِ error» بر «پیامِ message»). دیگر هیچ فایلی این ماژول را import
   نمی‌کند. طبق قاعدهٔ «حذف نکن، بازنشسته کن»، بدنهٔ قبلی (۲۹۳ سطر) پاک شد اما در
   تاریخچهٔ گیت باقی است؛ این فایل عمداً خالی نگه داشته می‌شود. */

export {};
